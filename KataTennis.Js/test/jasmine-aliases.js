/* globals window */
(function () {
  'use strict';

  window.context = beforeEach;
  window.establish = beforeEach;
  window.because = beforeEach;
  window.given = beforeEach;
  window.when = beforeEach;
  window.then = it;

})();